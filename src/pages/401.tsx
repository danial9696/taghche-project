import { Box } from '@mui/material';

/* The `Error401` function is a React component that renders the UI for the 401 error page. It returns
a JSX element that represents the structure and content of the error page. */
function Error401(): JSX.Element {
  return <Box>متاسفم. دسترسی‌ لازم برای استفاده از این صفحه رو نداری 🔐</Box>;
}

export default Error401;
