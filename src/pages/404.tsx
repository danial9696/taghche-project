import { Box } from '@mui/material';

/**
 * The function returns a JSX element displaying a server error message.
 * @returns A JSX element containing the text "خطای سرور داریم 🔐" wrapped in a Box component.
 */
function Error500(): JSX.Element {
  return <Box> خطای سرور داریم 🔐</Box>;
}

export default Error500;
